library constants;

import 'package:flutter/material.dart';

const Color LIGHT_GREY = Color.fromRGBO(116, 117, 121, 1);
const Color DARK_GREY = Color.fromRGBO(73, 74, 78, 1);
const Color TEXT_COLOR = Colors.white;

/// Name of the file which triggers an auto upload of all log files.
const String AUTO_UPLOAD_TRIGGER_FILE = '_SUCCESS';
